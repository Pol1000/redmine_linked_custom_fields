<% if @filtered_custom.present? %>
<p>
  <label><%=l(:label_valore)%></label>
  <select id="issue_linked_custom_id" name="issue[linked_custom_id]">
     <option value=""><%=l(:label_nobody)%></option> + <%= options_from_collection_for_select(@filtered_custom, :id, :valore) %>
  </select>
  <%=observe_field :issue_linked_custom_id, :url => {:controller => 'linked_custom_fields', :action => :update_form_issue2},
                                     :update => :secondo_select,
                                     :with => :linked_custom_id %>       
 <div id='secondo_select'></div>
 </p>
<% end %>