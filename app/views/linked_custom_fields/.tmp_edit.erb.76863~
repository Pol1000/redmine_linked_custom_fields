<h2><%=l(:label_valore)%></h2>

<% labelled_tabular_form_for :type, @custom, :url => {:action => 'update', :id => @custom} do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>

<%= submit_tag l(:button_save) %>
<%= link_to l(:button_cancel), :controller => 'linked_custom_fields', :action => 'gestione' %>
<% end %>
